# Homebrew tap for Trajectory

Install [Trajectory](https://github.com/asoules/trajectory), a local performance
review for Codex tasks:

```sh
brew install asoules/tap/trajectory
```

Upgrade to the latest release with `brew upgrade trajectory`.
